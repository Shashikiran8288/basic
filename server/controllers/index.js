const productsmaster = require('./productsmaster');
const products = require('./products');
const users = require('./users');
const transactions = require('./transactions');
const inventories = require('./inventories');

module.exports = {
	productsmaster,
    products,
    users,
    transactions,
    inventories
};